<!DOCTYPE html><html><head><title>Dashboard</title><link rel="stylesheet" href="/style.css"></head>
<body>
    <div class="layout"><%- include('partials/sidebar') %>
        <div class="content">
            <h1>Welcome, <%= user.username %></h1>
            <% if (!user.discordId) { %>
                <div class="card" style="border-left: 5px solid #5865F2; display:flex; justify-content:space-between; align-items:center;">
                    <div><h3>🔗 Link Discord</h3><p>Connect your account to track stats.</p></div>
                    <a href="/auth/discord" class="btn" style="background:#5865F2; text-decoration:none;">Connect</a>
                </div>
            <% } else { %>
                <div class="card" style="border-left: 5px solid #00e676;">
                    <h3>✅ Discord Linked</h3>
                    <% if(user.avatar){ %><img src="https://cdn.discordapp.com/avatars/<%= user.discordId %>/<%= user.avatar %>.png" style="width:30px; border-radius:50%; vertical-align:middle;"><% } %>
                    Stats tracking enabled.
                </div>
            <% } %>
            <div style="display: flex; gap: 20px;">
                <div class="card" style="flex:1"><h3>Cooked</h3><h1><%= stats ? stats.cook_count_total : 0 %></h1></div>
                <div class="card" style="flex:1"><h3>Delivered</h3><h1><%= stats ? stats.deliver_count_total : 0 %></h1></div>
            </div>
        </div>
    </div>
</body></html>
